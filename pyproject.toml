[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py310"
line-length = 100
exclude = ["notebooks", "outputs", "data"]

[tool.ruff.lint]
select = ["E", "F", "I"]
